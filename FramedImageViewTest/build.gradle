apply plugin: 'android'

dependencies {
    compile project(':FramedImageView')
}

android {
    compileSdkVersion 19
    buildToolsVersion "19.0.0"

    defaultConfig {
        minSdkVersion 1
        targetSdkVersion 19
    }
}
